type PostList {
  entries: [Post]
  pageInfo: PageInfo
}

type Scope {
  scopeId: Int!
  codename: String!
  name: String!

  post(postId: Int!): Post!
  posts: PostList
}

extend type Query {
  scope(codename: String): Scope!
  scopes: [Scope]
}
