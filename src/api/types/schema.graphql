scalar JSON
scalar Date
scalar Time
scalar DateTime

type PageInfo {
  totalCount: Int
  totalPages: Int
  currentPage: Int
  pageSize: Int
  hasNextPage: Boolean
}

type Query {
  _empty: String
}

type Mutation {
  _empty: String
}

type Subscription {
  _empty: String
}
